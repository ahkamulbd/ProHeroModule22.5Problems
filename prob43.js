/*
    43) তিনটা সংখ্যা এর য োগ করতে পারবে এমন একটা ফাংশন লি খ ো
*/

function addNumber(number1, number2, number3) {
    const additionNumbers = number1 + number2 + number3;

    return additionNumbers;
}

const result = addNumber(35, 45, 55);
console.log(result);